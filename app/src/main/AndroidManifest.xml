<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.prasunmondal.tech4docs">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tech4Docs">
        <activity android:name=".activities2.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.DisplayRecordTypeDetails" />
        <activity android:name=".activities2.PasswordPage"></activity>
        <activity android:name=".activities.ListRecordTypes" />
        <activity android:name=".activities.CustomizeRecordTypes" />
        <activity
            android:name=".activities.ViewCards"
            android:label="@string/title_activity_view_cards"
            android:theme="@style/Theme.Tech4Docs.NoActionBar" />
        <activity android:name=".activities.ActivityAddCards" />
        <activity android:name=".activities.MainActivity" />
    </application>

</manifest>