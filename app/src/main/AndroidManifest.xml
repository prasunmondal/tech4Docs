<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.prasunmondal.tech4docs">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tech4Docs">
        <activity android:name=".models.PasswordPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.ListRecordTypes">
        </activity>
        <activity android:name=".activities.ConfigureDataType"></activity>
        <activity
            android:name=".activities.ViewCards"
            android:label="@string/title_activity_view_cards"
            android:theme="@style/Theme.Tech4Docs.NoActionBar" />
        <activity android:name=".activities.ActivityAddCards"></activity>
        <activity android:name=".activities.MoneyMinded" />
        <activity android:name=".activities.MainActivity" />
    </application>

</manifest>